apiVersion: v1
kind: Service
metadata:
  name: parallelcs
  namespace: parallelcs
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: parallelcs
  ports:
    - name: http
      port: 80
      targetPort: http
